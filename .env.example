# Roostoo Mock Exchange API Credentials
API_KEY=your_api_key_here
SECRET_KEY=your_secret_key_here

# Optional: Set to "true" to skip placing real orders (logs what would be placed)
DRY_RUN=false

# Optional: Override log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
